/**
 * Typed, validated access to Vite environment variables.
 *
 * Re-exports from src/config/env so that services can import from either path.
 * The canonical source of truth is src/config/env.ts.
 */
export { env } from '../src/config/env';
